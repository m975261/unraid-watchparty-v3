services:
  watchparty-v3:
    image: muaeabudhabi/watchparty-v3:latest
    restart: unless-stopped
    labels:
      com.centurylinklabs.watchtower.enable: 'true'
      org.titan.app: watchparty-v3
    environment:
    - NODE_ENV=production
    - LOG_LEVEL=info
    - PUID=99
    - PGID=100
    - UMASK=022
    - DATABASE_URL=postgresql://watchparty-v3:changeme@watchparty-v3_postgres:5432/watchparty-v3
    - REDIS_URL=redis://watchparty-v3_redis:6379
    volumes:
    - /mnt/user/appdata/watchparty-v3/data:/data
    - /mnt/user/appdata/watchparty-v3/config:/config
    logging:
      driver: json-file
      options: &id001
        max-size: 10m
        max-file: '3'
    networks:
    - watchparty-v3_net
    depends_on:
      watchparty-v3_postgres:
        condition: service_healthy
      watchparty-v3_redis:
        condition: service_healthy
  watchparty-v3_postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
    - POSTGRES_DB=watchparty-v3
    - POSTGRES_USER=watchparty-v3
    - POSTGRES_PASSWORD=changeme
    volumes:
    - watchparty-v3_postgres_data:/var/lib/postgresql/data
    networks:
    - watchparty-v3_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER:-app}
      interval: 30s
      timeout: 5s
      retries: 5
  watchparty-v3_redis:
    image: redis:7-alpine
    restart: unless-stopped
    environment: []
    volumes:
    - watchparty-v3_redis_data:/data
    networks:
    - watchparty-v3_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD
      - redis-cli
      - ping
      interval: 30s
      timeout: 5s
      retries: 5
networks:
  watchparty-v3_net:
    driver: bridge
volumes:
  watchparty-v3_data:
    driver: local
  watchparty-v3_postgres_data:
    driver: local
  watchparty-v3_redis_data:
    driver: local
